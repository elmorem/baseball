# Baseball Stats Application Environment Variables
# Copy this file to .env and fill in the values

# Secret key for JWT token signing (minimum 32 characters)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-super-secret-key-at-least-32-characters-long

# OpenAI API Key for player descriptions
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key

# Database URL (used for local development outside Docker)
# Docker Compose uses its own internal URL
DATABASE_URL=postgresql+asyncpg://baseball_user:baseball_pass@localhost:5432/baseball_db
